# Node.js 관련 파일
node_modules/
package-lock.json

# 환경 변수 파일 (보안)
.env

# 로그 파일
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS별 임시 파일
.DS_Store
Thumbs.db

# Firebase 설정 파일 (민감한 정보 포함 가능)
.firebase/
.firebaserc

# Firestore 관련 파일 (배포 후 Git에 저장할 필요 없음)
firestore.indexes.json
firestore.rules

# Firebase 설정 파일 (자동 생성됨, Git에 불필요)
firebase.json
